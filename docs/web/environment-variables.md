# Web: 環境変数
ローカル開発時は `apps/web/.env` ファイルを作成して設定してください。

| 変数名 | 設定例 | 説明 |
| --- | --- | --- |
| `PORT` | `3000` | 開発中のサーバーの起動ポート番号を指定します。 |
| `JWT_SECRET` | `local-dev-secret` | API 用トークンの署名鍵。`apps/api/.env` と同じ値にします。未設定時は開発用の既定値を使います。 |
